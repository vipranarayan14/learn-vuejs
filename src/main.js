const { Vue } = window;

const app = new Vue({
  el: "#app",
  data: {
    product: "Socks",
    description: "A pair of warm, fuzzy socks.",
    image: "./assests/vmSocks-green-onWhite.jpg",
  },
});
